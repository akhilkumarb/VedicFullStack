<form>

  <div class="flex gap-2 py-4">
  
  <div class="flex-auto w-64">
  <input type="text" class="border border-gray-300 text-gray-900 text-sm rounded w-full p-2" placeholder="whats'next" autofocus required>
  </div>
  <div class="flex-auto w-32">
  <input type="date" class="border border-gray-300 text-gray-900 text-sm rounded w-full p-2 leading-4 ">
  </div>
  <div class="flex-none">
  <button type="submit" class="text-white bg-green-600 hover:bg-green-700 font-medium rounded text-sm px-5 py-2 mr-2 mb-2">Add</button>
  </div>
  </div>
  
  </form><!-- Overdue Section -->
<h5>Overdue</h5>
<ul class="list-none pl-4">
  <% for(var i = 0; i < allTodos.length; i++) { %>
    <% var dateString = allTodos[i].dueDate; %>
    <% var [year, month, day] = dateString.split('-'); %>
    <% var hello = ((year < new Date().getFullYear() && month < new Date().getMonth() + 1 && day < new Date().getDate()) ||
      (year == new Date().getFullYear() && month < new Date().getMonth() + 1 && day < new Date().getDate()) ||
      (year == new Date().getFullYear() && month == new Date().getMonth() + 1 && day < new Date().getDate())); %>
    <% if (hello) { %>
      <li class="Todo-Item">
        <div class="flex items-center w-fit my-2 px-2 py-1 rounded hover:bg-purple-50">
          <input id="todo-checkbox-overdue-<%= i %>" type="checkbox" value="" class="w-4h-4 text-blue-600 bg-gray-100 rounded border-gray-300">
          <label for="todo-checkbox-overdue-<%= i %>" class="ml-2 text-sm text-gray-600 cursor-pointer"><%= allTodos[i].title %></label>
          <a class="hidden trash-icon" href="#!">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round"
                d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
            </svg>
          </a>
        </div>
      </li>
    <% } %>
  <% } %>
</ul>

<!-- Due Today Section -->
<h5>Due Today</h5>
<ul class="list-none pl-4">
  <% for(var i = 0; i < allTodos.length; i++) { %>
    <% var dateString = allTodos[i].dueDate; %>
    <% var [year, month, day] = dateString.split('-'); %>
    <% var hello = (year == new Date().getFullYear() && month == new Date().getMonth() + 1 && day == new Date().getDate()); %>
    <% if (hello) { %>
      <li class="Todo-Item">
        <div class="flex items-center w-fit my-2 px-2 py-1 rounded hover:bg-purple-50">
          <input id="todo-checkbox-dueToday-<%= i %>" type="checkbox" value=""
            class="w-4h-4 text-blue-600 bg-gray-100 rounded border-gray-300">
          <label for="todo-checkbox-dueToday-<%= i %>" class="ml-2 text-sm text-gray-600 cursor-pointer"><%= allTodos[i].title %></label>
          <a class="hidden trash-icon" href="#!">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor"
              stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round"
                d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
            </svg>
          </a>
        </div>
      </li>
    <% } %>
  <% } %>
</ul>

<!-- Due Later Section -->
<h5>Due Later</h5>
<ul class="list-none pl-4">
  <% for(var i = 0; i < allTodos.length; i++) { %>
    <% var dateString = allTodos[i].dueDate; %>
    <% var [year, month, day] = dateString.split('-'); %>
    <% var hello = ((year > new Date().getFullYear()) || (year == new Date().getFullYear() && month > new Date().getMonth() + 1) ||
      (year == new Date().getFullYear() && month == new Date().getMonth() + 1 && day > new Date().getDate())); %>
    <% if (hello) { %>
      <li class="Todo-Item">
        <div class="flex items-center w-fit my-2 px-2 py-1 rounded hover:bg-purple-50">
          <input id="todo-checkbox-dueLater-<%= i %>" type="checkbox" value=""
            class="w-4h-4 text-blue-600 bg-gray-100 rounded border-gray-300">
          <label for="todo-checkbox-dueLater-<%= i %>" class="ml-2 text-sm text-gray-600 cursor-pointer"><%= allTodos[i].title %></label>
          <a class="hidden trash-icon" href="#!">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor"
              stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round"
                d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
            </svg>
          </a>
        </div>
      </li>
    <% } %>
  <% } %>
</ul>
